{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run data_loader",
            "type": "python",
            "request": "launch",
            "module": "Src.data_loader",
            "justMyCode": true,
            "cwd": "${workspaceFolder}"
        }
    ]
}
